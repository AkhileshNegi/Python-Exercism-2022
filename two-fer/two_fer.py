def two_fer(name='you'):
    '''
    :param name: str of a root name
    :return:  returns string with either name entered or default value `you` being replaced in string
    This function takes `name` as a parameter and
    returns string.
    '''
    return 'One for {name}, one for me.'.format(name=name)
